{% set metrics_list = [] %}
{% for node in groups['pulsar_cluster_core'] if hostvars[node].bookie is defined and hostvars[node].bookie|bool %}
{% set ignored = metrics_list.append(node + ':' + bk_stats_port|string) %}
{% endfor %}
{{ metrics_list | join(', ') }}